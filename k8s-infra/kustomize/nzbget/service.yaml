apiVersion: v1
kind: Service
metadata:
  name: nzbget
  namespace: plex
spec:
  type: LoadBalancer
  selector:
    app: nzbget
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 6789
